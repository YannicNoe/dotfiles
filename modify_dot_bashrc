if ! grep -Fxq 'if [ -f "$HOME/.config/entry.d/bash.sh" ]; then . $HOME/.config/entry.d/bash.sh; fi' $HOME/.bashrc ; then 
  echo 'if [ -f "$HOME/.config/entry.d/bash.sh" ]; then . $HOME/.config/entry.d/bash.sh; fi' >> $HOME/.bashrc ; 
fi

